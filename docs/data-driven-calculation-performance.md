# Data-driven calculation performance

Date: 2026-07-16

This report continues from `docs/mutation-archetype-performance.md`. It treats the earlier choice identity, batching, targeted catalog/list/card updates, conflict indexing, structural reconciliation batching, selective invalidation, derived-request coalescing, and persistence coalescing as established. Those paths were used as controls, not re-investigated.

Desktop measurements use Chromium without CPU throttling. Mobile Chromium uses Pixel 7 emulation with 4x CPU throttling. A p95 is shown only for five-sample runs. All other measurements are explicitly marked diagnostic and report the observed sample, not a percentile.

## 1. Existing performance knowledge

The versioned knowledge ledger is stored in `.artifacts/mutation-archetypes.json` and is generated by `scripts/audit-mutation-archetypes.mjs`. It currently contains 61 scenario/test records and retains evidence from 148 performance summaries.

| Ledger status | Records | Interpretation |
|---|---:|---|
| `mapped-and-optimized` | 36 | Correctness and attributed performance are understood; regression checks only. |
| `mapped-with-known-remaining-cost` | 6 | The path and remaining cost are understood, but a conservative or intrinsically structural cost remains. |
| `correctness-tested-but-not-performance-mapped` | 1 | Character switching has correctness coverage but no representative specialized-state timing. |
| `scenario-does-not-match-real-user-action` | 12 | The retained scenario changes state through test code rather than the production control. |
| `unmapped` | 3 | Artifact state synchronization, bundle removal, and nested movement still lack a reliable real-UI scenario. |
| `device-gap` | 3 | Large specialized character switching and some mobile/PWA attribution remain incomplete. |

The earlier report remains the baseline for choice/variant identity, choice cancellation, identical-variant batching, multi-buy, simple quantity batching, targeted catalog/character cards, conflict replacement, indexed conflict sources, grant/cascade batching, selective invalidation, worker coalescing, and the one-commit/one-refresh/one-persistence shape. Lightweight controls confirmed those mechanisms remain active:

- conflict replacement used no full render and spent 1.9 ms in grant planning, 1.9 ms in requirement evaluation, 1.6 ms in five conflict evaluations, and 0.1 ms in replacement planning;
- a structural artifact cascade used a targeted render and one commit; its remaining rule work is discussed below;
- the full unit, rule-validation, data-validation, mutation, parity, mobile, and PWA suites remained regression protection rather than discovery workloads.

The investigation did not repeat the already documented catalog-add and structural-list profiling unless a control was needed to attribute a newly instrumented shared stage.

## 2. Behavior-signature inventory

The audit normalizes each catalog entry into four parts:

1. structure: catalog types, stackability, individual/top-level shape, container, vehicle, bundle, catalog/custom identity, and runtime UID requirement;
2. variant behavior: choice fields and duplicate policy, variant-defining metadata, quality support, artifact binding, snapshot, and hidden/revealed state;
3. rules: requirements, grants, conflicts, modify targets, list-wide dependencies, and inventory/money grants;
4. expected impact: inventory, list, economy, traits, combat, effects, capacity, corruption, artifacts, snapshots, visibility, persistence, or unknown.

An action signature combines the entry signature with the production mutation (`add`, quantity change, removal, level change, quality change, movement, load/unload, reveal/snapshot, reload) and its expected impact. The current catalog produces 148 distinct entry signatures and 816 distinct action signatures across 1,538 entries. Every group, member count, deterministic representative set, and expected impact is retained in the machine artifact.

Representative selection is deterministic: first by frequency, then greatest relevant rule/state count, then a rare classified feature, then a measured outlier when one exists. Names are test labels only; production classification does not branch on them.

| Overlapping behavior family | Catalog entries | Representative behavior | Expected impact | Current path |
|---|---:|---|---|---|
| Simple local | 1,047 | Stable catalog row with no structural rule coupling | Row/list entry, totals when inventory, persistence | Targeted existing path. |
| Choice | 16 | Stackable and individual choice variants | Variant identity, row/structure, declared rule domains | Targeted when identity and impact are stable. |
| Variant-bearing | 155 | Choice, quality, artifact, or other variant metadata | Row/structure, economy, declared derived domains | Quantity and supported quality changes are targeted; ambiguous metadata falls back. |
| Batch/multi | 14 | Multi-buy, bundle/grant batch, repeated variant | Union of affected rows/domains | One root batch/commit; unknown mixed topology falls back. |
| Rule/reconciliation | 419 | Requirement, grant, conflict, or modify rules | List structure, grants, declared derived domains | Existing indexed/batched path; list-wide and ambiguous rules remain conservative. |
| Coupled list/inventory | 152 | Inventory grants, artifact/list coupling, snapshots | List, inventory, economy, artifacts/snapshots | Known grants are batched; artifact/snapshot ambiguity falls back. |
| Complex inventory topology | 276 | Individual items, containers, vehicles, bundles | Inventory structure/totals and persistence | Stable top-level rows and proven direct vehicle moves are targeted; other topology retains the full path. |
| Custom/legacy/unknown | 0 static entries; 76 import mappings | Runtime custom/imported rows | Unknown | Full safe path unless identity, schema, and impact are all proven. |

Special signature counts are: 8 stackable choices, 2 snapshot entries, 321 hidden/revealed entries, 2 bundles, 10 vehicles, 13 containers, and 261 individual entries. Catalog data contains no entry without an ID; missing IDs are a runtime/import concern.

Known modify targets are derived from the runtime `registerMal` dispatch table, not catalog names or a second hand-maintained list. Rule results are cached only on the current entry object and canonical level. Cache records include the catalog array reference, size, and `__entryDataVersions.db` generation; a changed reference or generation creates a fresh bounded per-entry map. Character-dependent requirement/conflict decisions are not retained across mutations.

## 3. Coverage gaps

The new real-UI scenarios close the largest gap for existing metadata-bearing rows: quantity increase/decrease, final-copy removal, and an economy-only quality addition now use their production buttons and popups. They cover warm and unavailable aggregate snapshots, 250 unrelated inventory rows, active filters, forced safe/optimized comparison, persistence flush, and reload.

Remaining gaps are:

- artifact reveal/hide/bind and snapshot synchronization: no reliable scenario yet follows the complete production UI action through all coupled state;
- bundle removal and nested movement: no real-UI diagnostic completed; retained simulations do not prove production-handler cost;
- container deletion/unwrap and specialized vehicle removal: retained scenarios mutate through test code; only real vehicle load/unload is performance-mapped;
- quality removal and declared combat/trait/effect quality targets: correctness paths exist, but only economy-only add/remove has forced-safe parity and reliable performance attribution;
- large specialized character switching: correctness exists for ordinary switching, but the new large-state scenario did not produce a reliable completed sample;
- structural level changes: existing mapped optimization remains a control, but the attempted new mobile diagnostic did not complete reliably enough to publish;
- Mobile WebKit performance: correctness parity passes, but no reliable timing was collected for the changed scenarios; Mobile Chromium was the diagnostic device because CPU/render amplification dominated;
- PWA: correctness and interaction budgets pass. No evidence identified a service-worker-specific mutation bottleneck, so no separate PWA performance matrix was run.

The audit marks old custom-entry, money, heavy-save, generic inventory removal, container, vehicle-removal, tag-removal, and inventory-clear simulations as `scenario-does-not-match-real-user-action`. They remain useful correctness/setup helpers, but not evidence about click latency.

Instrumentation now attributes rule source lookup/normalization, requirements, conflicts, grants/dependents, list-wide checks, inventory normalization/path/UID/flattening/aggregates/artifacts, derived request/queue/worker/apply, rendering/sort/card/DOM work, persistence preparation/materialization/transaction, hidden-surface refreshes, and canonical fallback reasons. No second profiler was introduced.

## 4. Most expensive newly measured user actions

Sections 4-9 preserve the findings and handoff state from the metadata-focused pass. Sections 10-12 contain the later topology/filter measurements and supersede the earlier vehicle/filter recommendations.

### Reliable safe-path versus optimized-path comparisons

All rows below use 250 inventory entries and five samples. “Safe” is the deliberately forced conservative control, not a claim that a normal production action always falls back.

| Action and path | Device | Visible median / p95 | Consistency median / p95 | Dominant stages and work |
|---|---|---:|---:|---|
| Metadata quantity increase, optimized | Desktop | 63.1 / 64.9 ms | 77.9 / 81.2 ms | 5.5 ms targeted surface patch; 1 UID validation; 1 aggregate delta. |
| Metadata quantity increase, forced safe | Desktop | 157.8 / 159.4 ms | 234.3 / 242.3 ms | 69.7 ms sort/group; full normalization, 2 scans, full UID traversal, 2 artifact scans, 3 flattens, worker request, 10,310 descendants replaced. |
| Metadata quantity increase, optimized | Mobile Chromium | 109.2 / 117.3 ms | 159.2 / 167.7 ms | 30.0 ms targeted surface work; no full render or worker. |
| Metadata quantity increase, forced safe | Mobile Chromium | 863.3 / 933.9 ms | 1,436.1 / 1,667.0 ms | 494.9 ms sort/group; same unrelated full work and 10,310 descendants replaced. |
| Final-copy removal, optimized | Desktop | 136.8 / 175.8 ms | 147.3 / 184.2 ms | Targeted structural card removal plus the intentional removal animation; 47 descendants removed. |
| Final-copy removal, forced safe | Desktop | 483.8 / 498.8 ms | 525.7 / 539.2 ms | 236.2 ms inventory render and 147.7 ms sort/group; 10,310 descendants replaced. |
| Final-copy removal, optimized | Mobile Chromium | 271.1 / 421.7 ms | 331.6 / 493.4 ms | Targeted structural removal; one aggregate delta and no worker. |
| Final-copy removal, forced safe | Mobile Chromium | 1,381.2 / 1,771.2 ms | 2,099.2 / 2,553.0 ms | 1,193.3 ms inventory render and 795.3 ms sort/group; 10,310 descendants replaced. |
| Economy-only quality add, optimized | Desktop | 37.0 / 105.4 ms | 41.6 / 110.4 ms | 1.9 ms targeted patch; 3 descendants replaced; one aggregate delta. |
| Economy-only quality add, forced safe | Desktop | 140.4 / 143.6 ms | 212.1 / 219.8 ms | 127.0 ms inventory render and 78.0 ms sort/group; full normalization/scans/artifact/worker; 10,305 descendants replaced. |
| Economy-only quality add, optimized | Mobile Chromium | 59.8 / 81.4 ms | 86.3 / 108.2 ms | 8.9 ms targeted patch; 3 descendants replaced. |
| Economy-only quality add, forced safe | Mobile Chromium | 607.0 / 650.7 ms | 971.1 / 992.8 ms | 548.0 ms inventory render and 339.3 ms sort/group; 10,305 descendants replaced. |

Each optimized action kept one root batch, one store mutation/common commit, one refresh generation, and one persistence schedule. Quantity and final removal preserved the row/variant identity rules; final removal preserved semantic `inventory.structure` invalidation while selecting `targeted-remove` as a separate render strategy.

### Second discovery pass

These are single diagnostic samples; no p95 is reported.

| Rank | Real UI action/signature | Device/state | Visible | Complete consistency | Fallback or path | Dominant attribution |
|---:|---|---|---:|---:|---|---|
| 1 | Vehicle load, structural topology | Mobile Chromium, 250 rows | 1,409.8 ms | 2,057.3 ms | `vehicle-topology` | 1,063.9 ms full inventory render, 694.4 ms sort/group, 10,304 descendants replaced. |
| 2 | Vehicle unload, structural topology | Mobile Chromium, 250 rows | 627.7 ms | 957.9 ms | `vehicle-topology` | 575.0 ms full inventory render, 347.0 ms sort/group, 10,309 descendants replaced. |
| 3 | Artifact grant/dependent cascade removal | Desktop, rule-heavy list control | 269.0 ms | 272.5 ms | Existing targeted path | 22.5 ms grant planning, 20.7 ms requirement-dependent discovery, 9.9 ms grant-dependent discovery. |
| 4 | Economy quality add with active filter | Mobile Chromium, 250 rows | 208.2 ms | 232.3 ms | `active-filter-membership-uncertain` | 176.2 ms full filtered render; only 46 descendants/5 cards rebuilt, showing the filter contains the DOM cost but prevents a proven patch. |
| 5 | Conflict replacement | Desktop, rule-heavy control | 187.5 ms | 192.2 ms | Existing incremental path | Rule evaluation/normalization below 2 ms per attributed family; no full render. |
| 6 | Final removal with cold aggregate | Desktop, 250 rows | 140.3 ms | 193.6 ms | Targeted render plus `aggregate-snapshot-unavailable` | One 46.7 ms aggregate/totals surface rebuild and one flatten; no full inventory render. |
| 7 | Economy quality add with cold aggregate | Desktop, 250 rows | 35.4 ms | 70.7 ms | Targeted render plus `aggregate-snapshot-unavailable` | One 32.1 ms aggregate/totals rebuild and one flatten. |

A desktop 20-row vehicle-load control produced 89.1 ms visible and 116.6 ms consistent state, versus the 250-row mobile result above. Device and size both differ, so this is evidence of severe mobile/large-state amplification, not a clean scaling coefficient. A dedicated same-device scaling series is still required before changing vehicle topology behavior.

Active-filter final removal also retained its exact fallback in a desktop diagnostic: 157.4 ms visible and 163.0 ms consistent. Warm/cold aggregate and active-filter behavior survived persistence/reload parity.

## 5. Most expensive shared calculations

| Rank | Shared calculation | Calls/contribution observed | Conclusion |
|---:|---|---|---|
| 1 | Full inventory sorting, grouping, card construction, and DOM replacement | At 250 rows, 273-278 cards and about 10,305-10,310 descendants were recreated. Mobile stages reached 1,193.3 ms render + 795.3 ms sort/group for removal and 1,063.9 + 694.4 ms for vehicle load. | Dominant broad cost and strongest mobile amplifier. Fixed for proven local quantity/removal, economy quality, and the later direct vehicle topology signature; retained for unproven topology. |
| 2 | Broad inventory preparation reached by safe paths | Per simple safe action: 1 inventory normalization, 2 inventory scans, 1 full UID normalization, 2 artifact scans, and 3 flatten operations. | Mostly unrelated to a stable top-level row. Eliminated from the new local/declared paths. |
| 3 | Unnecessary derived work on broad inventory saves | Safe controls issued 1 derived request and 1 worker request even though quantity/economy quality did not affect derived domains. | Eliminated by the impact contract for optimized signatures. Worker compute was not the dominant stage; request avoidance is still generally correct. |
| 4 | Aggregate rebuilding when the snapshot is unavailable | One flatten/rebuild added 32.1 ms for cold quality and 46.7 ms for cold final removal at 250 rows. | Real unrelated-state scaling remains, but the action stays targeted and correct. Optimize later only if cold frequency is significant. |
| 5 | Grant and dependent discovery for structural removal | 5 grant-planning calls/22.5 ms, 1 grant-dependent discovery/9.9 ms, 1 requirement-dependent discovery/20.7 ms containing 4 requirement evaluations/20.0 ms. | Largest measured rule-calculation cluster, but it belongs to an already targeted structural control and is correctness-sensitive. Needs scaled rule-source/dependent fixtures before another cache/index. |
| 6 | Conflict/requirement/retrieval normalization | Conflict control: 51 cache lookups, 35 hits, 16 cold misses/normalizations totaling 1.7 ms; 5 conflicts/1.6 ms, requirements/1.9 ms, grant planning/1.9 ms. Cascade control: 24 lookups, 23 hits, 1 normalization/0.4 ms. | Repeated retrieval exists, but measured normalization is not a leading bottleneck. No mutation-spanning character-dependent cache was added. |
| 7 | Persistence preparation and serialization | Queue preparation was normally 0-0.6 ms; one field clone/materialization and one schedule per optimized action. IndexedDB transactions ranged from about 1-8 ms desktop and 10-83 ms mobile, safely after visible feedback. | Not a synchronous bottleneck in the measured mutations. Existing coalescing remains. |
| 8 | Hidden-surface refresh, duplicate refresh, and duplicate persistence | No hidden-surface refresh counter appeared in the changed scenarios; optimized paths recorded exactly one refresh generation and one persistence schedule. | No evidence for an additional optimization. |

Artifact-effect scanning was not individually dominant, but its two calls per broad safe mutation were provably unrelated for the optimized signatures and are now skipped. Price, weight, food, and quantity totals use row deltas when a compatible snapshot exists. Money-weight and capacity stay on the existing aggregate calculation because a row-only mutation does not always prove that those inputs are independent.

## 6. Data-driven root causes

The root cause was not one catalog entry. It was the combination of a simple UI mutation with metadata or topology that caused the old save/render path to assume broad impact:

- choice metadata plus quantity entered inventory normalization, full UID traversal, artifact scanning, full sort/group/card rebuild, and an unrelated derived request even when the existing variant identity did not change;
- final-copy removal correctly meant `inventory.structure`, but rendering treated every structural invalidation as requiring a full inventory rebuild. Semantic impact and render strategy had been conflated;
- quality metadata used the same broad path even when every runtime-registered modify target declared only price/weight/economy impact;
- active filters require a before/after membership/order classifier; the later continuation proves only quantity/economy-quality `remain` cases;
- nested inventory, containers, vehicles, and bundles can change parentage, capacity/category membership, or multiple rows. The later continuation proves only a direct top-level↔vehicle move;
- artifact plus list/snapshot/hidden state can couple an inventory row to list entries, revealed IDs, effects, and snapshot sources;
- grant plus requirement/dependent removal is correctly broader than a row mutation; dependent discovery is measurable but remains behaviorally significant;
- unknown modify targets, manual overrides, custom entries, and legacy metadata cannot safely declare a narrow domain;
- a missing aggregate snapshot forces a complete totals rebuild even though rendering remains local.

Mobile makes the fallback disproportionately worse because JavaScript sorting/model construction, DOM creation/replacement, and layout all amplify under 4x CPU throttling. Persistence transactions also grow on mobile, but they did not block first visible feedback and were much smaller than rendering.

## 7. Implemented general improvements

### Mutation-impact contract and semantic/render separation

`js/inventory-utils.js`, `js/store.js`, and `js/main.js` now carry a single internal impact contract with `local`, `declared`, or `fallback` classification; affected semantic domains; identity and topology guarantees; aggregate strategy; render strategy; derived invalidation; and exact fallback reasons.

Semantic invalidation is not weakened to obtain a fast render. A known top-level final removal still invalidates `inventory.structure`, `inventory.totals`, `summary.economy`, and persistence, while independently selecting `targeted-remove`. Different render strategies within one batch escalate safely to full rendering.

### Stable top-level quantity and final removal

`js/inventory-utils.js` updates an existing row without recreating variant identity, validates only the affected UID (or the expected absence after removal), applies price/weight/food/quantity deltas, patches the known card and totals, and retains one common commit/refresh/persistence schedule. It skips full quality normalization, vehicle partitioning, artifact scans, flattening, and derived work only when classification proves they cannot change.

Cold aggregates use the exact `aggregate-snapshot-unavailable` reason and rebuild totals without turning the mutation into a full render. Unproven active-filter membership/order, unstable identity, missing UID, nested/container/unproven vehicle/bundle topology, artifact/snapshot coupling, rules, custom/legacy data, and DOM postcondition failures retain precise full fallbacks.

The reliable measurements in section 4 show the result. On Mobile Chromium, quantity visible p95 fell from the forced-safe 933.9 ms to 117.3 ms and complete-consistency p95 from 1,667.0 to 167.7 ms. Final removal visible p95 fell from 1,771.2 to 421.7 ms and consistency p95 from 2,553.0 to 493.4 ms.

### Declared economy-only quality changes

The second optimized family is quality/metadata mutation whose runtime-registered targets declare only inventory row/totals/economy impact. `js/inventory-utils.js` now patches quality chips, details/info, price, weight, and totals; applies a before/after aggregate row delta; and avoids the full render/artifact/derived pipeline.

Known combat, trait, effect, or summary targets currently retain `declared-quality-impact-unoptimized`. Artifact, snapshot, structure, custom, manual, legacy, unknown target, nested path, unproven filter membership/order, and missing UID cases retain the safe path. No representative name appears in production conditions.

On Mobile Chromium, economy-quality visible p95 fell from the forced-safe 650.7 ms to 81.4 ms and consistency p95 from 992.8 to 108.2 ms. Desktop visible p95 fell from 143.6 to 105.4 ms; the larger p95 variance in the optimized sample is presentation scheduling, while its median fell from 140.4 to 37.0 ms and its attributed surface work from 127.0 to 1.9 ms.

### Rule cache lifecycle and attribution

`js/rules-helper.js` now records source resolution, cache lookup/hit/miss, normalization, requirements, conflicts, grants, dependents, cascade checks, modify-target mapping, and list-wide checks. The existing entry/level cache is guarded by catalog reference, size, and generation. Measurements showed high warm hit rates and sub-2 ms normalization in the conflict control, so no speculative cross-mutation requirement/conflict cache was implemented.

### Rendering, persistence, and profiling

`js/perf.js`, `js/persistence.js`, `js/index-view.js`, and `js/inventory-utils.js` extend `window.symbaroumPerf` with user-visible milestones, outer-stage timing, inner call counts, DOM/card work, hidden surfaces, persistence preparation/transaction/flush, and canonical fallback counts. `scripts/run-scenario-metrics.mjs` supplies the real-UI scenarios, device/state dimensions, cold aggregates, filters, and forced safe controls. `scripts/audit-mutation-archetypes.mjs` supplies the versioned ledger/signatures and retains the measurements.

### Correctness parity and cache invalidation

`tests/inventory-mutation-parity.spec.js` executes the real production controls twice from the same normalized state: first with the forced safe path, then with the optimized path. It compares inventory topology and row identity, variant metadata and quantity/qualities, money, artifact effects, derived state where applicable, rendered cards/totals, persistence flush, and reload. Quantity/final-removal and economy-quality add/remove pass in Desktop Chromium, Mobile Chromium, and Mobile WebKit. Character switching is not part of these cases because the optimized paths introduce no character-dependent cache and issue no derived worker request.

Generated legacy bundles were updated only after source changes and targeted tests.

## 8. Remaining expensive fallbacks before the topology continuation

| Exact reason | Measured cost | Affected signatures | Why it remains / evidence needed |
|---|---|---|---|
| `vehicle-topology` | Mobile load 1,409.8 ms visible/2,057.3 ms consistent; unload 627.7/957.9 ms, one sample each | Vehicle, nested inventory, capacity/category coupling | Moving a row changes parentage, vehicle category, capacity, and possible nested layout. Needs same-device 20/100/250 scaling, normalized topology parity, and a verifiable targeted category postcondition. |
| `active-filter-membership-uncertain` | Quality add 208.2/232.3 ms Mobile; final removal 157.4/163.0 ms Desktop, one sample each | Any row/quality mutation under inventory filters | A changed quality/row may enter, leave, or reorder the filtered set. Needs a shared filter-membership classifier and DOM parity for every active filter/sort combination. |
| `aggregate-snapshot-unavailable` | Quality cold rebuild added 32.1 ms; final-removal cold rebuild 46.7 ms at 250 rows | Local/declared inventory mutations after cache loss | Correct targeted fallback, not a full render. Optimize only with evidence that cold snapshots are frequent and with parity for money-weight/capacity/requirements. |
| `individual-instance` | No dedicated reliable latency; observed in the non-stackable choice control | Individual/non-stackable variants | Instance creation/removal may carry unique metadata. Needs stable instance identity and per-instance parity before a structural patch. |
| `nested-inventory-path`, `container-topology`, `bundle-expansion` | Real-UI timings still unavailable | Containers, nested rows, two catalog bundles, multi-row expansion/removal | Parent/child and multi-row topology are not provably local. Need production-control scenarios at depths 1/3/5 and full tree/reload parity. |
| `artifact-list-sync`, `snapshot-sync`, `hidden-revealed-state` | Real-UI synchronization timing unavailable | Artifact, revealed/hidden, and snapshot-bearing signatures | Inventory/list/effect/revealed/snapshot coupling is ambiguous. Need end-to-end reveal/hide/bind/sync scenarios and normalized list/inventory/artifact parity. |
| `rule-reconciliation-required`, `list-wide-dependency` | Cascade control 269.0/272.5 ms; attributed dependent work up to 20.7 ms, one sample | Grants, dependents, requirements, list-wide rules | These calculations are semantically broad. Need scaled rule-source/dependent fixtures and proof that an index remains valid for the whole root mutation. |
| `unknown-modify-target`, `declared-quality-impact-unoptimized` | No reliable production-control timing | Custom/legacy qualities and declared derived targets | Unknown targets stay full. Known derived targets need worker/full-path parity and per-domain rendered-state checks before narrowing. |
| `custom-entry`, `legacy-metadata`, `manual-override`, `missing-row-uid`, `unstable-variant-identity` | Retained scenarios are not real UI and therefore not published as latency evidence | Runtime custom/import/manual data | Schema/identity/intent is not stable enough for a local postcondition. Need canonical migration/identity guarantees and real-control parity. |
| `target-dom-card-missing`, `dom-postcondition-failed` | Not observed in reliable optimized samples | Any targeted render | These are mandatory runtime safety valves. They remain full fallbacks by design. |

Forced-safe controls account for 30 recorded fallback activations but are excluded from production-frequency ranking. Among observed production reasons, the retained artifact ranks `aggregate-snapshot-unavailable` (3), `vehicle-topology` (3), and `active-filter-membership-uncertain` (2). The counts describe this diagnostic corpus, not population usage frequency.

Recommended future order is:

1. same-device vehicle/container/nested scaling and topology parity, because it has the largest measured mobile latency;
2. filter-membership classification for quality/row changes, because the fallback is common and its rendered set is already bounded;
3. cold aggregate profiling in real sessions before considering a narrower rebuild/index;
4. scaled grant/requirement dependent discovery before adding another rule index or mutation-scoped decision cache;
5. artifact/snapshot real-UI mapping, then large specialized character switching;
6. custom/legacy optimization only after stronger schema and identity guarantees.

Insufficiently understood areas remain explicitly marked `unmapped`, `device-gap`, or `scenario-does-not-match-real-user-action` in the ledger rather than being presented as optimized.

## 9. Plain-language conclusion

The genuinely slow behavior was a mismatch between what the user changed and what the application rebuilt. Changing one quantity, removing one known row, or adding an economy-only quality could rebuild and replace an entire 250-row inventory, rescan artifacts, normalize every UID, and ask the derived worker to run. Mobile magnified that broad JavaScript and DOM work into roughly one to two seconds of visible or consistency delay.

The application now decides semantic impact separately from how the UI should render it. A final removal is still truthfully structural, but a known top-level card can be removed directly. Stable metadata quantities keep their identity and update aggregates by delta. Economy-only quality changes patch only the affected card and totals. These changes cover broad data-driven signature classes and contain no entry-name special cases.

The second discovery pass found that vehicle load/unload was the largest measured remaining user-facing cost and handed it to the continuation below. Sections 10-12 resolve the narrow direct vehicle signature and proven-remain filters; artifact/snapshot coupling, bundles, containers, nested inventory, unknown rules, custom/legacy rows, and other filter membership/order changes retain explicit safe fallbacks.

Users should feel the largest improvement when adjusting metadata-bearing quantities, removing their final copy, or adding/removing economy-only qualities—especially in large mobile inventories. The topology continuation below applies the same correctness-first method to direct vehicle movement without treating other structural states as proven.

## 10. Structural inventory-topology continuation

This continuation treats the quantity, final-copy removal, and economy-only quality work above as established controls. It investigates structural parentage changes, beginning with the two available real production controls: vehicle load and vehicle unload. No production condition branches on a catalog name.

### Clean same-device baseline

The pre-change series used three samples for each size/action pair. Load and unload each move one row across one parent boundary and affect one source category plus the vehicle category. Depth and affected-row/category counts therefore remain fixed while unrelated rows and visible cards scale.

| Device | Rows | Action | Visible median | Consistency median | Aggregate | Sort/group + cards | Descendants replaced | Layout / style |
|---|---:|---|---:|---:|---:|---:|---:|---:|
| Desktop Chromium | 20 | Load | 63.8 ms | 75.8 ms | 7.0 ms | 16.4 ms | 831 | 5.5 / 7.9 ms |
| Desktop Chromium | 20 | Unload | 33.9 ms | 44.5 ms | 6.6 ms | 14.9 ms | 831 | 4.4 / 6.1 ms |
| Desktop Chromium | 100 | Load | 92.3 ms | 120.7 ms | 16.2 ms | 36.6 ms | 3,833 | 12.6 / 16.8 ms |
| Desktop Chromium | 100 | Unload | 69.1 ms | 98.0 ms | 16.7 ms | 35.1 ms | 3,833 | 11.7 / 14.2 ms |
| Desktop Chromium | 250 | Load | 171.6 ms | 240.4 ms | 31.3 ms | 70.0 ms | 10,304 | 27.8 / 35.0 ms |
| Desktop Chromium | 250 | Unload | 160.4 ms | 237.2 ms | 37.2 ms | 84.9 ms | 10,304 | 29.0 / 32.1 ms |
| Mobile Chromium | 20 | Load | 214.7 ms | 299.2 ms | 31.9 ms | 81.3 ms | 831 | 27.8 / 45.9 ms |
| Mobile Chromium | 20 | Unload | 171.2 ms | 225.9 ms | 35.8 ms | 73.3 ms | 831 | 16.2 / 28.1 ms |
| Mobile Chromium | 100 | Load | 438.8 ms | 597.3 ms | 80.8 ms | 168.6 ms | 3,833 | 57.4 / 87.5 ms |
| Mobile Chromium | 100 | Unload | 341.9 ms | 584.8 ms | 83.2 ms | 173.4 ms | 3,833 | 91.0 / 107.1 ms |
| Mobile Chromium | 250 | Load | 723.9 ms | 1,039.6 ms | 129.6 ms | 290.2 ms | 10,304 | 113.4 / 164.0 ms |
| Mobile Chromium | 250 | Unload | 552.2 ms | 863.0 ms | 136.8 ms | 301.9 ms | 10,304 | 109.2 / 141.6 ms |

Planning, row/path lookup, and the actual tree splice were 0-2.2 ms in this baseline. Every action instead reconstructed all 20/100/250 cards, replaced the complete inventory root, requested one derived/worker generation, and performed one refresh and persistence schedule. The dominant coefficient is therefore total unrelated inventory rows/visible cards, amplified by layout and style on mobile—not the one affected row, two categories, or measured depth.

### Pure topology mutation plan

`planInventoryTopologyMutation` is constructed before state mutation. It records the source UID and variant identity; source/destination parents and paths; source/vehicle categories; vehicle capacity and aggregate deltas; empty/new parent/category transitions; exact cards to move, preserve, or refresh; expected top-level, parent, and category order; semantic invalidations; render strategy; and exact fallback reasons. Tests compare the serialized inventory before and after plan construction.

The proven vehicle signature is deliberately narrow: one complete stable catalog row moves directly between the top level and a direct vehicle child list; every UID is present and unique; aggregate state is warm; no money is moved in the same batch; no stack merge is required; and artifact, snapshot, hidden, custom, legacy, rule-coupled, container, bundle, manual, partial-stack, nested, and filtered topology is absent. The plan preserves semantic invalidation of `inventory.structure`, `inventory.totals`, `capacity`, `summary.economy`, and `persistence` while independently selecting `targeted-move`.

### Targeted topology reconciliation

The targeted reconciler applies the planned tree splice in one store mutation/common commit, validates the moved UID recursively, applies an aggregate topology delta, and moves the existing card node. It creates or removes only the local vehicle child shell/category structure, patches vehicle weight/capacity metadata, updates local path datasets and ordering, and rebinds motion only for the two affected lists. It never clears or replaces the inventory root.

Postconditions verify exactly one runtime row and one card; the planned path and parent; absence from the source parent; top-level, vehicle-child, and category order; empty parent/category visibility; and category order. Failure records the precise condition and immediately invokes the existing full renderer. Runtime safety reasons include `target-dom-root-missing`, `target-dom-card-missing`, `target-dom-card-duplicate`, `source-dom-parent-mismatch`, `moved-row-cardinality-failed`, `moved-row-path-failed`, `vehicle-parent-path-failed`, `top-level-order-failed`, `parent-order-failed`, `moved-card-cardinality-failed`, `parent-card-cardinality-failed`, `moved-card-parent-failed`, `empty-parent-visibility-failed`, `category-order-failed`, and the general `dom-postcondition-failed` guard.

The architecture recognizes `targeted-move`, `targeted-parent-refresh`, and `targeted-category-refresh` as render strategies without weakening structure/totals/capacity/economy/persistence invalidations. Only `targeted-move` is selected by this pass; the other strategy names reserve explicit escalation points rather than implementing an unmeasured reconciler.

### Optimized scaling and safe controls

The final series pairs the production optimized action with a deliberately forced safe path on the same device. The 20/100-row results use three samples; 250 rows use five and therefore include p95.

| Device | Rows | Action | Optimized visible | Optimized consistency | Plan / tree / DOM reconcile | Cards rebuilt / preserved |
|---|---:|---|---:|---:|---:|---:|
| Desktop Chromium | 20 | Load | 60.3 ms | 64.2 ms | 1.4 / 3.7 / 2.0 ms | 0 / 20 |
| Desktop Chromium | 20 | Unload | 61.4 ms | 65.9 ms | 1.5 / 2.6 / 1.4 ms | 0 / 20 |
| Desktop Chromium | 100 | Load | 32.2 ms | 37.5 ms | 1.5 / 3.6 / 2.2 ms | 0 / 100 |
| Desktop Chromium | 100 | Unload | 47.5 ms | 53.3 ms | 1.9 / 3.1 / 1.9 ms | 0 / 100 |
| Desktop Chromium | 250 | Load | 64.7 ms | 76.7 ms | 1.6 / 6.8 / 3.7 ms | 0 / 250 |
| Desktop Chromium | 250 | Unload | 24.7 ms | 29.0 ms | 2.3 / 4.7 / 2.7 ms | 0 / 250 |
| Mobile Chromium | 20 | Load | 77.4 ms | 90.3 ms | 6.2 / 13.0 / 7.7 ms | 0 / 20 |
| Mobile Chromium | 20 | Unload | 39.2 ms | 57.9 ms | 6.3 / 11.1 / 6.5 ms | 0 / 20 |
| Mobile Chromium | 100 | Load | 130.6 ms | 148.4 ms | 6.0 / 14.6 / 9.1 ms | 0 / 100 |
| Mobile Chromium | 100 | Unload | 46.6 ms | 69.5 ms | 7.2 / 12.9 / 6.8 ms | 0 / 100 |
| Mobile Chromium | 250 | Load | 250.5 / 509.9 ms p95 | 287.3 / 589.3 ms p95 | 6.9 / 21.8 / 14.3 ms | 0 / 250 |
| Mobile Chromium | 250 | Unload | 76.8 / 78.4 ms p95 | 99.2 / 100.5 ms p95 | 10.8 / 19.3 / 11.4 ms | 0 / 250 |

At 250 rows, the paired forced-safe versus optimized medians/p95 are:

| Device/action | Safe visible median / p95 | Optimized visible median / p95 | Safe consistency median / p95 | Optimized consistency median / p95 |
|---|---:|---:|---:|---:|
| Desktop load | 181.1 / 295.9 ms | 64.7 / 81.7 ms | 266.5 / 412.8 ms | 76.7 / 91.2 ms |
| Desktop unload | 149.9 / 179.6 ms | 24.7 / 27.9 ms | 232.3 / 259.6 ms | 29.0 / 33.8 ms |
| Mobile load | 794.5 / 826.1 ms | 250.5 / 509.9 ms | 1,152.4 / 1,188.0 ms | 287.3 / 589.3 ms |
| Mobile unload | 618.5 / 629.0 ms | 76.8 / 78.4 ms | 965.9 / 986.5 ms | 99.2 / 100.5 ms |

The 250-row optimized medians reduce complete consistency by 71.2% for desktop load, 87.5% for desktop unload, 75.1% for mobile load, and 89.7% for mobile unload. The three-sample desktop 20-row load result is fixed-overhead/paint noise rather than a regression in broad work: it still rebuilds zero cards and performs no full render/worker request. The optimized planner intentionally retains an O(total-tree) uniqueness and expected-order validation, visible in the modest 20-to-250 growth; full sorting/grouping/card construction is absent.

### DOM, layout, and UI stability

At 250 rows, forced-safe load created 10,309 observed nodes, removed/replaced about 10,304 descendants, and rebuilt all 250 cards. Forced-safe unload created about 10,304, removed 10,309, replaced 10,304, and rebuilt all cards. Optimized load created 8 local shell nodes, moved one card (38 observed nodes including its subtree), removed/replaced 2 nodes, reconstructed 0 cards, and preserved all 250 card nodes. Optimized unload created 2, moved the same one-card subtree, removed 7 local shell nodes, replaced 2, reconstructed 0, and preserved all 250 cards.

Baseline-to-optimized layout/style work at 250 rows fell from 27.8/35.0 to 4.6/11.0 ms for desktop load, 29.0/32.1 to 2.1/3.1 ms for desktop unload, 113.4/164.0 to 17.7/49.9 ms for mobile load, and 109.2/141.6 to 6.8/18.3 ms for mobile unload.

Every optimized sample preserved scroll position and expanded category/parent state; kept exactly one source and vehicle card; kept every unrelated card node; never emptied the root; produced no duplicate UID/card; never showed the row in an intermediate wrong parent; recorded zero inventory animation passes and zero layout shift; and matched the forced-safe focus transition caused by reopening the production vehicle popup. It used one targeted UID validation, one refresh generation, one persistence schedule, no full UID normalization, no unrelated worker request, and no derived version. Motion bindings were replaced only on the affected source/destination lists; no duplicate observer or second inventory animation pass was observed.

### Parity and conservative generalization

Vehicle load and unload pass forced-safe versus optimized comparisons for the normalized complete inventory tree, parent-child relationships, UID/variant identity, quantities/metadata, capacity, price/weight totals, money, artifact/derived state, visible cards, category/parent structure, focus/scroll, persistence, and reload. The focused suite passes Desktop Chromium, Mobile Chromium, and Mobile WebKit. WebKit was used for correctness/UI stability only; no WebKit performance matrix was created because behavior did not diverge.

The same pure planner was exercised against a simple row nested in a container, a move from depth 3, container unwrap, and bundle removal. All remained non-mutating `full-fallback` plans. The exact retained reasons are `nested-inventory-path` for the nested moves; `unsupported-topology-path` plus `container-topology` for unwrap; and `unsupported-topology-path` plus `bundle-expansion` for bundle removal. These families were not generalized: the retained container scenarios mutate the store through test code rather than a production control, and nested/bundle production controls remain absent.

Other topology plan fallbacks include `forced-safe-path`, `active-filter-membership-uncertain`, `aggregate-snapshot-unavailable`, `multi-row-topology-unproven`, `topology-and-money-batch-unsupported`, `unknown-topology-metadata`, `source-row-not-found`, `destination-parent-not-found`, `unsupported-topology-parent`, `topology-cycle`, `missing-row-uid`, `duplicate-row-uid`, `unstable-variant-identity`, `custom-entry`, `legacy-metadata`, `manual-override`, `snapshot-sync`, `artifact-list-sync`, `hidden-revealed-state`, `rule-reconciliation-required`, `list-wide-dependency`, `unknown-modify-target`, `partial-stack-topology`, and `destination-stack-merge-uncertain`.

## 11. Active-filter membership continuation

Filtering now uses one shared pure predicate/classifier for both full rendering and mutation planning. It evaluates the current text filter against row name, trait, and children; type/tradition/test tags against catalog metadata; union/intersection mode; before/after membership; category; parent; and inventory index. It classifies `remain`, `leave`, `enter`, or `outside`, and independently reports position changes and the required `targeted-patch`, `targeted-remove`, `targeted-insert`, `targeted-none`, or `targeted-move` strategy.

The first filter pass implemented only the provable `remain` plus unchanged-position strategy. Quantity changes and economy-only quality metadata do not participate in the filter predicate or ordering, so a visible stable top-level card can be patched. The continuation below adds one more exactly provable case: deletion of a visible stable top-level row, whose after-state membership is necessarily false and whose absence/order postcondition can be checked. Unknown input retains `active-filter-membership-uncertain`; unimplemented membership changes retain `active-filter-leave-unoptimized`, `active-filter-enter-unoptimized`, or `active-filter-outside-unoptimized`; ordering changes retain `active-filter-order-change-unoptimized`. Topology movement under filters remains conservative.

The existing active-filter quantity production regression continues to preserve the card and avoid a full render. Economy-only quality add/remove now passes forced-safe/optimized/reload parity with an active text filter and preserves the card node. At 250 rows on Mobile Chromium, five filtered quality-add samples measured 204.7/206.9 ms visible median/p95 and 228.0/235.2 ms consistency on the forced-safe path, versus 31.2/33.6 ms visible and 45.3/48.9 ms consistency optimized. The safe path spent 168.5 ms in the filtered full render and replaced 46 descendants; optimized replaced 3 local descendants, issued no worker/derived generation, and kept one refresh and persistence schedule.

## 12. Completion state and remaining priorities

Optimized topology signatures are direct, full-row, stable catalog moves between top level and a vehicle, in both load and unload directions, with warm aggregates and the safety conditions above. Measured but intentionally conservative topology includes container/nested movement, depth-3 movement, unwrap, bundle removal, active filters, multi-row/partial-stack movement, money plus topology, stack merges, artifact/snapshot/hidden coupling, custom/legacy/manual data, and ambiguous rules.

Filter cases optimized are proven-remain quantity and economy-only quality changes plus deletion of one proven visible stable top-level row. General enter/leave/outside/reorder and topology under filters retain full fallback. Artifact/snapshot and custom/legacy optimization is explicitly deferred.

Files changed in this continuation are `js/inventory-utils.js`, `js/store.js`, `js/main.js`, generated `js/legacy/shared.js` and `js/legacy/post.js`, `scripts/run-scenario-metrics.mjs`, `tests/inventory-topology-parity.spec.js`, `tests/inventory-mutation-parity.spec.js`, this report, and the regenerated mutation-archetype ledger.

Remaining bottlenecks, in priority order, are:

1. production-control mapping for container moves/unwrap, nested moves, and bundle removal before any broader topology reconciliation;
2. the retained O(total-tree) uniqueness/order validation in targeted topology plans, but only if real large-tree frequency shows it matters;
3. cold aggregate rebuilds after cache loss;
4. filtered enter/leave/reorder strategies, only with production controls and explicit DOM/order postconditions;
5. scaled grant/requirement dependent discovery;
6. artifact/reveal/snapshot production synchronization;
7. custom/legacy optimization after stronger schema and identity guarantees.

No evidence changed the earlier conclusions about rule-cache redesign, broad persistence architecture, worker transport, character-dependent rule caching, hidden-surface rendering, or the complete mutation pipeline, so those areas were not refactored.

### Final verification accounting

The focused topology suite passes load/unload forced-safe versus optimized and reload parity in Desktop Chromium, Mobile Chromium, and Mobile WebKit. Its Chromium run also passes the pure/non-mutating conservative planner cases for a simple container move, depth-3 movement, unwrap, and bundle removal. The active-filter economy-quality parity case passes in Chromium. Build, lint, 83 unit tests, strict catalog/schema/migration validation, and rule verification also pass. The Chromium PWA release gate passes 15/15.

The one-time broad release runs were not completely green, and their exact accounting is retained rather than hidden. The final Desktop Chromium/WebKit run completed with 191 passed, 4 skipped, and 9 failed. The starting-commit comparison and final classifications are recorded in section 16. The incremental-add stage assertion and OLD_VERSION import count assertion were corrected after direct behavioral accounting and now pass in both engines. No production change was made to unrelated CSS, geometry, scroll, screenshots, or catalog-add behavior to force their assertions green.

The completed responsive comparison run had 87 passed, 4 skipped, and 7 failed: coarse-pointer sizing at 667 px and 768 px in both mobile engines, the same character-tools screenshot baseline in both, and one environment-sensitive Mobile Safari rapid-click assertion. Each failure was reproduced by the exact test on the starting commit. A requested final responsive rerun could not start after the local execution-approval quota was exhausted; the dedicated 18-test inventory parity matrix nevertheless passed on Mobile Chrome and Mobile Safari after the last production change. These release-suite failures are verification debt outside this pass, not optimized topology or filter cases presented as passing.

## 13. Vehicle-load presentation attribution

The original optimized 250-row Mobile Chromium load result was 250.5/509.9 ms visible median/p95 and 287.3/589.3 ms complete-consistency median/p95. More detailed checkpoints separated production-control activation, confirmation, handler entry, popup close/reconciliation, focus, plan construction, UID/order/capacity/aggregate validation, child-shell creation, vehicle-card patching, DOM reconciliation, style/layout, first correct paint, and final consistency.

The attributed pre-change median work was:

| Stage | Median |
|---|---:|
| Pure topology planning / complete plan | 0.8 / 6.5 ms |
| UID / expected-order validation | 0.5 / 1.1 ms |
| Capacity / aggregate calculation | 0.1 / 0.1 ms |
| Vehicle child shell / metadata-card patch | 0.2 / 1.3 ms |
| Targeted inventory DOM reconciliation | 11.6 ms |
| Popup close | 3.1 ms |
| Popup reconstruction after the move | 146.3 ms |
| Layout / style over the full traced interaction | 16.3 / 46.1 ms |

Capacity, aggregates, O(total-tree) UID/order validation, shell creation, and vehicle-card patching together were far too small to explain the load/unload gap. The load-specific cause was reconstruction of every candidate control in the still-open production popup, followed by its focus/presentation scheduling. Unload reconstructed a much smaller popup. The measured shared fix therefore applies only to the already-proven direct vehicle-load signature: popup rows/options receive stable UID and parent/order attributes, the one moved popup row is reconciled in place, group counts/capacity are patched, and exact popup membership/order postconditions are checked. Failure retains `vehicle-popup-postcondition-failed` and reconstructs the complete popup.

At 250 rows, five final Mobile Chromium samples measured 58.5/70.4 ms visible and 98.0/145.9 ms complete consistency. That is a 76.6% median and 86.2% p95 visible reduction, and a 65.9% median and 75.2% p95 consistency reduction from the prior optimized result. The final medians were 1.5 ms topology planning, 7.2 ms complete plan, 0.7 ms UID validation, 1.5 ms order validation, 0.1 ms capacity, 0.5 ms aggregate, 0.2 ms shell creation, 1.8 ms vehicle metadata patch, 12.1 ms inventory DOM reconciliation, and 10.7 ms popup reconciliation. Layout/style fell to 7.4/26.8 ms. Every sample rebuilt zero cards, replaced two local descendants, used one refresh and one persistence schedule, and reached correct paint/final consistency without fallback.

Opening the load control before confirmation still costs about 599 ms at 250 rows because it initially creates the complete candidate list. That cost is outside confirmation-to-visible latency and remains measured, not hidden. It was not optimized because the requested investigation isolated the post-confirmation load/unload difference and no shared proof yet supports virtualizing or otherwise changing popup construction.

## 14. Real-UI topology family map

The diagnostic mapping uses the production load/unload buttons, the supported unload-then-load transfer sequence, container delete/`deleteContainerOnly`, and the index bundle removal control. One sample was taken at small and 250-row state on 4x-throttled Mobile Chromium; all cases flushed persistence and compared canonical state after reload.

| Rank | Production action | Small visible / consistency | 250-row visible / consistency | Full renders | Cards reconstructed | Descendants replaced | Aggregate | Layout / style | Exact fallback | Reload parity |
|---:|---|---:|---:|---:|---:|---:|---:|---:|---|---|
| 1 | Container delete-only / unwrap | 153.5 / 202.2 ms | 546.6 / 881.1 ms | 1 | 20 small; 250 large, plus removed shell | 823 / 10,305 | 34.2 / 127.5 ms | 14.6/32.8; 118.6/144.4 ms | `container-topology` | pass |
| 2 | Bundle removal (six root rows) | 94.2 / 107.5 ms | 376.3 / 406.9 ms | 1 | mounted inventory not visible | n/a | 15.6 / 86.3 ms | 2.9/8.1; 3.8/9.9 ms | `legacy-metadata` | pass |
| 3 | Supported nested transfer, second confirmation | 30.0 / 48.3 ms | 56.3 / 127.7 ms | 0 | 0 | 2 | 0 / 0 ms | 4.2/11.3; 8.7/19.5 ms | none | pass |
| 4 | Direct whole-row unload | 45.8 / 70.5 ms | 82.5 / 114.7 ms | 0 | 0 | 2 | 0.6 / 0.1 ms | 5.8/16.5; 6.2/18.4 ms | none | pass |
| 5 | Direct whole-row load | 43.1 / 59.0 ms | 58.5 / 98.0 ms | 0 | 0 | 2 | 0.5 ms large | 7.4/26.8 ms large | none | pass |

The nested sample moves one stable whole row out of one supported vehicle parent and into another through two real confirmations. Its first unload/control preparation was 673.7 ms small and 884.9 ms large; the measured second load control activation was 212.2/479.6 ms. It affected the row, two vehicle rows, root, source parent, and destination parent; optimized reconciliation preserved all 251 cards and exact parent/order membership.

There is no distinct production control for moving an arbitrary row into an ordinary non-vehicle storage container. The reliable whole-row in/out scenario therefore covers the application's supported vehicle-container UI, while the general simple-container family stays unresolved and conservative. No test-only store splice is presented as a real user action.

No additional topology family was optimized. Unwrap is clearly the slowest but reconstructs the whole mounted surface and changes scroll/focus; bundle removal is second but is a six-row batch reached through legacy/unclassified metadata. Neither yet has the exact source/destination, stable finite membership/order plan, mounted-UI postconditions, and forced-safe parity required for a new targeted reconciler. The pure planner still retains `nested-inventory-path`, `unsupported-topology-path`, `container-topology`, and `bundle-expansion` for directly submitted unsupported plans; production observations retain `container-topology` for unwrap and `legacy-metadata` for bundle removal.

## 15. Filtered membership continuation

The pure classifier was exercised for all requested membership shapes. Proven leave requires `targeted-remove`; enter requires `targeted-insert`; a remain case whose index changes requires `targeted-move`; outside requires no visible DOM action. The exact retained implementation reasons are `active-filter-enter-unoptimized`, `active-filter-order-change-unoptimized`, `active-filter-outside-unoptimized`, and `active-filter-membership-uncertain`. Topology under filters is still rejected.

Only deletion of a currently visible, stable top-level catalog row was implemented. It proves before membership, after absence, root parent, stable UID, and final visible order; removes the known card/category shell; preserves scroll, focus, and expansion state; and uses one refresh and one persistence schedule. At 250 rows on Mobile Chromium, five production-control samples measured 121.4/127.5 ms visible and 131.3/141.4 ms consistency, versus 274.3/278.0 and 292.9/298.3 ms forced safe. The optimized path removed one card, reconstructed zero, replaced six local descendants, spent 4.9/13.5 ms layout/style, and passed canonical persistence/reload parity. The forced-safe control retained `forced-safe-path`, one full inventory render, and 128.9 ms aggregate work.

General filtered enter and local reorder were not implemented because no stable, non-custom production control in the mounted filtered inventory simultaneously proves the mutation, insertion anchor, and final order. The requested filtered remove is the measured leave transition above. This pass does not infer a broader leave strategy from deletion and does not optimize topology changes under filters.

## 16. Release accounting and final evidence

The original comparison point is commit `e3a28c5`. Exact failed tests were run from a detached starting-commit worktree with the same browser/toolchain. The OLD_VERSION fixture was compared directly rather than labeled stale.

| Assertion family | Starting commit | Current/final evidence | Classification / verdict |
|---|---|---|---|
| Add-profile staged breakdown, Chromium/WebKit | fails: expected broad stage names | Incremental direct add intentionally performs form/store/common commit, aggregate, sort/group and DOM patch, but no worker round trip or derived-total recompute; corrected assertion passes both engines | intentionally changed behavior; **safe test update** |
| OLD_VERSION Testperson inventory count, Chromium/WebKit | fails 13 expected vs 7 actual | Source has 13 rows; six rows are fully granted by `Välutrustad` and reconciled away, while rope remains quantity 1 after 3 of 4 are granted. Exact remaining IDs are `di12`, `di10`, `di11`, `di13`, `di14`, `di15`, `l9`; corrected assertion passes both engines | pre-existing expected-state error; **safe test update**, not regression |
| 250-list/250-inventory direct-add p95 | isolated starting commit passes | Failed only in final two-worker sweep; isolated current tree also passes | nondeterministic/environment-sensitive; **pre-existing release-harness sensitivity** |
| Installed-PWA rule-warming direct-add p95 | reproduces in broad starting comparison | Final broad run fails; serial installed-PWA gate passes 15/15 | nondeterministic/environment-sensitive; **pre-existing failure** |
| Index category anchor, Chromium/WebKit | fails both | fails both final | deterministic **pre-existing failure** |
| Index scroll restoration | fails starting comparison | appeared in earlier broad run, passes serial/final | nondeterministic/environment-sensitive; **pre-existing failure** |
| Traits panel geometry | fails starting comparison | fails final Chromium | deterministic **pre-existing failure** |
| Character Tools 700 px/two-column geometry | fails starting comparison | fails final Chromium | deterministic **pre-existing failure** |
| Inventory manager / representative dialog screenshot | fails starting comparison in desktop and mobile engines | fails final Chromium/WebKit and prior Mobile Chrome/Safari run | deterministic baseline mismatch; **pre-existing failure** |
| Coarse-pointer sizing at 667 and 768 px | fails starting comparison in both mobile engines | fails completed responsive comparison | deterministic **pre-existing failure** |
| Five rapid WebKit/Mobile Safari trait clicks | fails starting comparison intermittently | final WebKit and completed responsive run fail; serial diagnostic passes | nondeterministic/environment-sensitive; **pre-existing failure** |

No broad-release assertion was classified as a newly introduced production regression. The final core result is 191 passed, 4 skipped, 9 failed; the serial installed-PWA gate is 15/15. The previously completed responsive comparison is 87 passed, 4 skipped, 7 failed and all seven failures reproduce at the starting commit. Its final rerun is the only unresolved verification item because the environment refused another local-server execution after the approval quota was exhausted. This limitation does not invalidate the later focused Mobile Chrome/Mobile Safari parity run, which passed as part of the 18/18 Desktop Chromium, Mobile Chromium, and Mobile WebKit matrix.

Build, lint, 83 unit tests, strict catalog/schema/migration validation, rule verification, mutation-ledger generation, focused forced-safe/optimized state/UI/persistence/reload parity, and PWA verification pass. Cold aggregate fallback was absent from the warm vehicle/filter/topology corpus and appeared only after deliberate snapshot invalidation in the cold controls, where it added 32.1-46.7 ms at 250 rows; no aggregate redesign was justified. The Mobile Chromium artifact/grant cascade measured 368.3 ms visible and 382.2 ms consistency, including 58.7 ms grant planning, 53.4 ms requirement-dependent discovery, and 20.9 ms grant-dependent discovery. Hidden-artifact inventory removal measured 32.1/41.4 ms but conservatively full-rendered with `legacy-metadata`. These measurements preserve the earlier decision not to redesign rule caching, persistence, worker transport, or the complete mutation pipeline.

Release verdict: the two assertion corrections are safe test updates; all remaining classified failures are pre-existing or environment-sensitive; there is no measured new production regression. The changes are suitable to commit and merge with the known baseline failures documented, but an organization that requires an absolutely green broad suite should first repair or formally quarantine those pre-existing release tests and rerun the blocked responsive gate. No commit or push was performed.
